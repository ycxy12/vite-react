import { createAction } from "@reduxjs/toolkit"

// 设置token
export const setToken = createAction("SET_TOKEN")

// 更新Collapse
export const updateCollapse = createAction("UPDATE_COLLAPSE")

// 设置菜单
export const setMenuList = createAction("SET_MENU_LIST")
